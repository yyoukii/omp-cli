## What does this change do?

## Checklist

- [ ] `npm run lint` passes
- [ ] `npm run format:check` passes
- [ ] `npm run build` passes
- [ ] I added an entry to `docs/DECISIONS.md` if this change involves a non-obvious trade-off